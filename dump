// function save(rowNumber) {
//     let editform = document.getElementById(`formNumber${rowNumber}`);
//     let nameEdit = editform.elements[`editName${rowNumber}`].value;
//     let stockEdit = editform.elements[`editStock${rowNumber}`].value;
//     let priceEdit = editform.elements[`editPrice${rowNumber}`].value;
//     produk[rowNumber].name = nameEdit;
//     produk[rowNumber].stock = stockEdit;
//     produk[rowNumber].price = parseInt(priceEdit).toLocaleString("id");
//     document.getElementById(`name${rowNumber}`).innerHTML = `${nameEdit}`;
//     document.getElementById(`stock${rowNumber}`).innerHTML = `${stockEdit}`;
//     document.getElementById(`price${rowNumber}`).innerHTML = `Rp. ${priceEdit}`;
//     document.getElementById(`button${rowNumber}`).innerHTML = '<button type="button" onclick = "edit(${i})">Edit</button><br><button type="button" onclick="deleteRow(${i})">Delete</button>'
// }

// function cancel(rowNumber){
//     //test
// }

// function edit(rowNumber) {
//     document.getElementById(`name${rowNumber}`).innerHTML = `<input type="text" id=editName${rowNumber}>`;
//     document.getElementById(`stock${rowNumber}`).innerHTML = `<input type="number" id=editStock${rowNumber}>`;
//     document.getElementById(`price${rowNumber}`).innerHTML = `<input type="number" id=editPrice${rowNumber}>`;
//     document.getElementById(`button${rowNumber}`).innerHTML = `<button type="button" onclick=save(${rowNumber})>Save</button><br><button type="button" onclick=cancel(${rowNumber})>Cancel</button>`
//     document.getElementById(`editName${rowNumber}`).placeholder = produk[rowNumber].name;
//     document.getElementById(`editStock${rowNumber}`).placeholder = produk[rowNumber].stock;
//     document.getElementById(`editPrice${rowNumber}`).placeholder = produk[rowNumber].price;
//     // document.getElementById(`name${rowNumber}`).contentEditable = true;
//     // document.getElementById(`stock${rowNumber}`).contentEditable = true;
//     // document.getElementById(`price${rowNumber}`).contentEditable = true;
// }